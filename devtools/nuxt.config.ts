// Nuxt SEO devtools panel, shipped as a layer (Model C). The unified devtools client
// (assembled by nuxtseo-shared in the user's project) extends this to render /seo-utils.
export default defineNuxtConfig({})
